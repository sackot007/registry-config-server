server:
  port: 8888

logging:
  level:
    org.springframework.cloud: 'DEBUG'

#eureka:
#  instance:
#    leaseRenewalIntervalInSeconds: 10
#    statusPageUrlPath: /admin/info
#    healthCheckUrlPath: /admin/health

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sackot007/registry-config

---
spring:
  profiles: cloud


